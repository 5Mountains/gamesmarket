import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    height: 150,
    overflow: 'hidden',
    borderRadius: 10,
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
