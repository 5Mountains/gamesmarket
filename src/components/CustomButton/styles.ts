import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    backgroundColor: '#ad40af',
    padding: 20,
    borderRadius: 10,
    marginBottom: 30,
  },
  buttonLabel: {
    textAlign: 'center',
    fontWeight: '700',
    color: '#ffffff',
    fontSize: 16,
  },
});
