import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  rootContainer: {
    flexDirection: 'row',
    borderBottomWidth: 1,
    borderBottomColor: '#cccccc',
    paddingBottom: 10,
    marginBottom: 25,
  },
  textField: {
    marginLeft: 5,
    flex: 1,
    paddingVertical: 0,
  },
  fieldLabel: {
    color: '#ad40af',
    fontWeight: '700',
  },
});
